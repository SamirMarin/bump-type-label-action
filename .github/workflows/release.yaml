name: release

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: bump_labels
        id: bump-label
        uses: SamirMarin/bump-type-label-action@v0.0.2

      - name: echo-test
        run: echo ${{ steps.bump-label.outputs.value }}


  
